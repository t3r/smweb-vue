# Dependencies
node_modules/
npm-debug.log*

# Environment
.env
.env.local

# Deploy-generated app version (see VERSION.example, scripts/resolve-git-slug.mjs)
VERSION

# Build outputs
dist/
build/

# Lambda deployment bundle (CI / local before terraform apply)
lambda/email-queue-worker/bundle.zip

# Terraform (local state / vars; commit .terraform.lock.hcl when present)
**/.terraform/
*.tfstate
*.tfstate.*
crash.*.log
terraform/email-queue-worker/terraform.tfvars

# Uploads
uploads/

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Logs
*.log
googlee7c627d062fa63de.html

.vite/
*.sw?

# Legacy PHP / static frontend (not used by the Node + Vite stack under src/)
/*.php
/classes/
/css/
/img/
/inc/
/map/
/rss/
/submission/
/svc/
/view/
