Hello,

Someone (the account "{{sourceName}}", id {{sourceAuthorId}}) started merging with the author "{{targetName}}" (id {{targetAuthorId}}) in the FlightGear Scenemodels catalogue — this message was sent to the target account email to verify you control it.

Next step: open this URL on the same device where you are signed in with the source OAuth account (the one that started the merge):

{{link}}

This link expires around {{expiresAt}}.

If you did not request this, ignore this email.

— FlightGear Scenemodels
